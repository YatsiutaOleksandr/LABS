<Window x:Class="LAB2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TextEditor" Height="400" Width="700"
        Background="#F4F6F8">

    <!-- СТИЛІ -->
    <Window.Resources>

        <!-- Стиль кнопок -->
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Стиль TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
        </Style>

    </Window.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- ПАНЕЛЬ КНОПОК -->
        <Border Grid.Row="0" Background="#E3EAF2" CornerRadius="10" Margin="5">
            <StackPanel Orientation="Horizontal" Margin="5"
                        FocusManager.IsFocusScope="True">

                <Button Content="Open" Command="Open"/>
                <Button Content="Clear" Command="Delete"/>
                <Button Content="Copy" Command="Copy"/>
                <Button Content="Paste" Command="Paste"/>

            </StackPanel>
        </Border>

        <!-- ОСНОВНА ЧАСТИНА -->
        <Grid Grid.Row="1" Margin="5">

            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ТЕКСТОВЕ ПОЛЕ -->
            <TextBox Name="inputTextBox"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     FontSize="{Binding ElementName=fontSlider, Path=Value}"
                     BorderThickness="1"
                     />

            <!-- НИЖНЯ ПАНЕЛЬ -->
            <Border Grid.Row="1" Background="#E3EAF2" CornerRadius="10" Margin="5">
                <DockPanel Margin="5">

                    <Label Content="Font:" VerticalAlignment="Center"/>

                    <Slider Name="fontSlider"
                            Minimum="12"
                            Maximum="50"
                            Width="120"
                            Margin="5"/>

                    <Button Content="Save"
                            Command="Save"
                            DockPanel.Dock="Right"/>

                </DockPanel>
            </Border>

        </Grid>

    </Grid>
</Window>